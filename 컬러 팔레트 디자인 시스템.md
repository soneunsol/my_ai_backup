# 🎨 컬러 팔레트 디자인 시스템

## 📝 프로젝트 정보
- **출처 웹사이트**: New Source Magazine
- **분석 날짜**: 2025-11-07
- **적용 프로젝트**: 포트폴리오 웹사이트
- **디자인 컨셉**: 모던 매거진 스타일

---

## 🎯 CSS 변수 정의

```css
:root {
  /* Primary Colors */
  --color-primary: #C41E3A;              /* 메인 레드 - 브랜드 색상 */
  --color-primary-light: #E63950;         /* 밝은 레드 - 호버 효과용 */
  --color-primary-dark: #A01729;          /* 어두운 레드 - 액티브 상태 */

  /* Secondary Colors */
  --color-secondary: #FFB300;             /* 골드 - 강조 요소 */
  --color-secondary-light: #FFC933;       /* 밝은 골드 */
  --color-secondary-dark: #E69D00;        /* 어두운 골드 */

  /* Accent Colors */
  --color-accent: #FFFFFF;                /* 화이트 - 주요 강조점 */
  --color-accent-soft: #FFF8F9;           /* 부드러운 화이트 */

  /* Background Colors */
  --color-bg-primary: #FFE5E8;            /* 메인 배경 - 연한 핑크 */
  --color-bg-secondary: #FFD1D6;          /* 보조 배경 - 중간 핑크 */
  --color-bg-tertiary: #FFC4CA;           /* 삼차 배경 - 진한 핑크 */
  --color-bg-white: #FFFFFF;              /* 순백 배경 */
  --color-bg-dark: #2D2D2D;               /* 다크 섹션용 */

  /* Text Colors */
  --color-text-primary: #2D2D2D;          /* 주요 텍스트 */
  --color-text-secondary: #5A5A5A;        /* 보조 텍스트 */
  --color-text-muted: #8E8E8E;            /* 비활성 텍스트 */
  --color-text-light: #FFFFFF;            /* 밝은 배경용 텍스트 */
  --color-text-accent: #C41E3A;           /* 강조 텍스트 */

  /* Border Colors */
  --color-border-light: #E0E0E0;          /* 밝은 테두리 */
  --color-border-medium: #CCCCCC;         /* 중간 테두리 */
  --color-border-accent: #C41E3A;         /* 강조 테두리 */

  /* Interactive Colors */
  --color-button-primary: #C41E3A;        /* 기본 버튼 */
  --color-button-primary-hover: #A01729;  /* 버튼 호버 */
  --color-button-secondary: #FFB300;      /* 보조 버튼 */
  --color-button-secondary-hover: #E69D00; /* 보조 버튼 호버 */

  --color-link: #C41E3A;                  /* 링크 */
  --color-link-hover: #A01729;            /* 링크 호버 */
  --color-link-visited: #8E1522;          /* 방문한 링크 */

  /* Shadow Colors */
  --color-shadow-light: rgba(196, 30, 58, 0.1);   /* 밝은 그림자 */
  --color-shadow-medium: rgba(196, 30, 58, 0.2);  /* 중간 그림자 */
  --color-shadow-dark: rgba(196, 30, 58, 0.3);    /* 진한 그림자 */

  /* Overlay Colors */
  --color-overlay-light: rgba(255, 229, 232, 0.8);  /* 밝은 오버레이 */
  --color-overlay-dark: rgba(45, 45, 45, 0.7);      /* 어두운 오버레이 */
}
```

---

## 🎨 컬러 사용 가이드

### 1️⃣ Primary Color (메인 레드)
**색상**: `#C41E3A`

**사용 위치**:
- 네비게이션 바 배경
- 메인 로고 텍스트
- 주요 CTA(Call-to-Action) 버튼
- 중요한 헤딩 텍스트
- 브랜드 아이덴티티가 필요한 모든 요소

**사용 예시**:
```css
/* 메인 헤더 */
.header {
  background-color: var(--color-primary);
  color: var(--color-accent);
}

/* CTA 버튼 */
.btn-primary {
  background-color: var(--color-button-primary);
  color: var(--color-accent);
}

.btn-primary:hover {
  background-color: var(--color-button-primary-hover);
}
```

---

### 2️⃣ Secondary Color (골드)
**색상**: `#FFB300`

**사용 위치**:
- 보조 CTA 버튼
- 아이콘 강조
- 특별 배지나 라벨
- 호버 효과 액센트
- 프리미엄 요소 표시

**사용 예시**:
```css
/* 특별 배지 */
.badge-premium {
  background-color: var(--color-secondary);
  color: var(--color-text-primary);
}

/* 아이콘 강조 */
.icon-featured {
  color: var(--color-secondary);
}
```

---

### 3️⃣ Background Colors (배경 색상)
**메인 배경**: `#FFE5E8`

**사용 가이드**:
- **Primary Background**: 전체 페이지 기본 배경
- **Secondary Background**: 카드, 섹션 구분
- **Tertiary Background**: 호버 상태, 강조 영역

**사용 예시**:
```css
body {
  background-color: var(--color-bg-primary);
}

.card {
  background-color: var(--color-bg-white);
  border: 1px solid var(--color-border-light);
}

.card:hover {
  background-color: var(--color-bg-secondary);
}

.section-alternate {
  background-color: var(--color-bg-tertiary);
}
```

---

### 4️⃣ Text Colors (텍스트 색상)
**주요 텍스트**: `#2D2D2D`

**가독성 가이드**:
- **Primary Text**: 본문, 헤딩, 주요 콘텐츠
- **Secondary Text**: 부가 정보, 캡션
- **Muted Text**: 날짜, 메타 정보
- **Accent Text**: 강조할 텍스트 (링크 등)

**사용 예시**:
```css
h1, h2, h3 {
  color: var(--color-text-primary);
}

p {
  color: var(--color-text-secondary);
}

.text-muted {
  color: var(--color-text-muted);
}

.text-highlight {
  color: var(--color-text-accent);
  font-weight: 600;
}
```

---

### 5️⃣ Interactive Elements (상호작용 요소)

**버튼 스타일**:
```css
/* Primary Button */
.btn-primary {
  background-color: var(--color-button-primary);
  color: var(--color-accent);
  padding: 12px 24px;
  border: none;
  border-radius: 4px;
  transition: all 0.3s ease;
}

.btn-primary:hover {
  background-color: var(--color-button-primary-hover);
  box-shadow: 0 4px 8px var(--color-shadow-medium);
  transform: translateY(-2px);
}

/* Secondary Button */
.btn-secondary {
  background-color: var(--color-button-secondary);
  color: var(--color-text-primary);
}

.btn-secondary:hover {
  background-color: var(--color-button-secondary-hover);
}

/* Outline Button */
.btn-outline {
  background-color: transparent;
  color: var(--color-primary);
  border: 2px solid var(--color-primary);
}

.btn-outline:hover {
  background-color: var(--color-primary);
  color: var(--color-accent);
}
```

**링크 스타일**:
```css
a {
  color: var(--color-link);
  text-decoration: none;
  transition: color 0.3s ease;
}

a:hover {
  color: var(--color-link-hover);
  text-decoration: underline;
}

a:visited {
  color: var(--color-link-visited);
}
```

---

## 📱 반응형 고려사항

### 다크모드 변형
```css
@media (prefers-color-scheme: dark) {
  :root {
    /* Dark Mode Overrides */
    --color-bg-primary: #1A1A1A;
    --color-bg-secondary: #2D2D2D;
    --color-bg-tertiary: #3A3A3A;
    --color-bg-white: #252525;

    --color-text-primary: #FFFFFF;
    --color-text-secondary: #CCCCCC;
    --color-text-muted: #8E8E8E;

    /* Primary colors remain the same for brand consistency */
    --color-primary: #E63950;  /* Slightly lighter for better contrast */
    --color-secondary: #FFC933;

    --color-border-light: #3A3A3A;
    --color-border-medium: #5A5A5A;
  }
}
```

### 모바일 최적화
```css
@media (max-width: 768px) {
  :root {
    /* Mobile-specific adjustments */
    /* Slightly increase contrast for smaller screens */
    --color-text-primary: #1A1A1A;
    --color-shadow-light: rgba(196, 30, 58, 0.15);
  }

  /* Touch-friendly button sizes */
  .btn-primary,
  .btn-secondary {
    padding: 14px 28px;
    font-size: 16px;
  }
}
```

### 고대비 모드 (접근성)
```css
@media (prefers-contrast: high) {
  :root {
    --color-primary: #B71C1C;  /* Darker for more contrast */
    --color-text-primary: #000000;
    --color-bg-primary: #FFFFFF;
    --color-border-light: #000000;
  }
}
```

---

## 🔧 실전 활용 예시

### 카드 컴포넌트
```css
.card {
  background-color: var(--color-bg-white);
  border: 1px solid var(--color-border-light);
  border-radius: 8px;
  padding: 20px;
  transition: all 0.3s ease;
}

.card:hover {
  background-color: var(--color-bg-secondary);
  border-color: var(--color-border-accent);
  box-shadow: 0 8px 16px var(--color-shadow-light);
  transform: translateY(-4px);
}

.card-title {
  color: var(--color-text-primary);
  font-size: 1.5rem;
  margin-bottom: 10px;
}

.card-description {
  color: var(--color-text-secondary);
  line-height: 1.6;
}

.card-badge {
  background-color: var(--color-secondary);
  color: var(--color-text-primary);
  padding: 4px 12px;
  border-radius: 12px;
  font-size: 0.875rem;
}
```

### 네비게이션
```css
.navbar {
  background-color: var(--color-primary);
  padding: 16px 32px;
  box-shadow: 0 2px 8px var(--color-shadow-medium);
}

.navbar-link {
  color: var(--color-accent);
  padding: 8px 16px;
  transition: all 0.3s ease;
}

.navbar-link:hover {
  background-color: var(--color-primary-dark);
  border-radius: 4px;
}

.navbar-link.active {
  border-bottom: 2px solid var(--color-secondary);
}
```

### 섹션 구분
```css
.section-hero {
  background: linear-gradient(135deg,
    var(--color-bg-primary) 0%,
    var(--color-bg-secondary) 100%);
  padding: 80px 20px;
}

.section-content {
  background-color: var(--color-bg-white);
  padding: 60px 20px;
}

.section-featured {
  background-color: var(--color-bg-tertiary);
  border-left: 4px solid var(--color-primary);
  padding: 40px 20px;
}
```

---

## ✅ 접근성 체크리스트

- [ ] **대비율**: WCAG AA 기준 이상 (최소 4.5:1)
  - Primary Red (#C41E3A) on White: ✅ 7.2:1
  - Text (#2D2D2D) on Light Pink (#FFE5E8): ✅ 12.1:1

- [ ] **색각 이상 고려**: 색상만으로 정보를 전달하지 않음
- [ ] **포커스 상태**: 모든 상호작용 요소에 명확한 포커스 표시
- [ ] **다크모드**: 야간 사용성 고려
- [ ] **고대비 모드**: 시각 장애 사용자 배려

---

## 📚 추가 참고사항

### 그라데이션 활용
```css
/* Hero Section Gradient */
.gradient-primary {
  background: linear-gradient(135deg, #FFE5E8 0%, #FFD1D6 50%, #FFC4CA 100%);
}

/* Accent Gradient */
.gradient-accent {
  background: linear-gradient(90deg, #C41E3A 0%, #FFB300 100%);
}

/* Subtle Overlay */
.overlay-subtle {
  background: linear-gradient(180deg,
    rgba(255, 229, 232, 0) 0%,
    rgba(255, 229, 232, 0.8) 100%);
}
```

### 애니메이션 효과
```css
/* Smooth Color Transitions */
.animated-element {
  transition:
    background-color 0.3s ease,
    color 0.3s ease,
    border-color 0.3s ease,
    box-shadow 0.3s ease;
}

/* Pulse Effect */
@keyframes pulse-accent {
  0%, 100% {
    box-shadow: 0 0 0 0 rgba(196, 30, 58, 0.4);
  }
  50% {
    box-shadow: 0 0 0 10px rgba(196, 30, 58, 0);
  }
}

.pulse {
  animation: pulse-accent 2s infinite;
}
```

---

## 🎯 마무리

이 컬러 시스템은 **모던하고 감성적인 매거진 스타일**을 구현하기 위해 설계되었습니다.

**핵심 포인트**:
1. **브랜드 일관성**: Primary Red를 일관되게 사용
2. **계층 구조**: 배경색의 단계적 사용으로 시각적 깊이 표현
3. **사용자 경험**: 부드러운 핑크 배경으로 편안한 느낌
4. **접근성**: WCAG 기준을 충족하는 대비율

**업데이트 이력**:
- 2025-11-07: 초기 컬러 시스템 구축
